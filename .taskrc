# [Created by task 2.5.3 10/21/2022 20:49:41]
# Taskwarrior program configuration file.
# For more documentation, see http://taskwarrior.org or try 'man task', 'man task-color',
# 'man task-sync' or 'man taskrc'

# Here is an example of entries that use the default, override and blank values
#   variable=foo   -- By specifying a value, this overrides the default
#   variable=      -- By specifying no value, this means no default
#   #variable=foo  -- By commenting out the line, or deleting it, this uses the default

# Use the command 'task show' to see all defaults and overrides

# Files
data.location=~/.task

# Color theme (uncomment one to use)
#include /usr/share/task/light-16.theme
#include /usr/share/task/light-256.theme
#include /usr/share/task/dark-16.theme
#include /usr/share/task/dark-256.theme
#include /usr/share/task/dark-red-256.theme
#include /usr/share/task/dark-green-256.theme
#include /usr/share/task/dark-blue-256.theme
#include /usr/share/task/dark-violets-256.theme
#include /usr/share/task/dark-yellow-green.theme
#include /usr/share/task/dark-gray-256.theme
#include /usr/share/task/dark-gray-blue-256.theme
include /usr/share/task/solarized-dark-256.theme
#include /usr/share/task/solarized-light-256.theme
#include /usr/share/task/no-color.theme

news.version=2.6.0

report.pjs.description=Projects
report.pjs.columns=id,project,description.count
report.pjs.labels=id,project,text
report.pjs.sort=priority,urgency-,project+
report.pjs.filter=status:pending -ACTIVE -WAITING -BLOCKED -OVERDUE -DUETODAY limit:10

report.doing.description=In progress
report.doing.columns=id,project,description.count
report.doing.labels=id,project,text
report.doing.sort=priority-
report.doing.filter=status:pending +ACTIVE limit:10

report.duenow.description=Projects
report.duenow.columns=id,project,description.count
report.duenow.labels=id,project,text
report.duenow.sort=priority-,urgency-,project+
report.duenow.filter=due:today -COMPLETED limit:10

color=on
